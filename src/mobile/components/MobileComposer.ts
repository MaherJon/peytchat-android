/**
 * mobile/components/MobileComposer — 移动端编辑器(兼容性重导出)
 *
 * 此文件保留用于向后兼容。
 * 实际实现在 compat/ui/composer.ts。
 * 所有新代码应直接从 compat/ui/composer.js 导入。
 */

export {
  renderComposer,
  type MobileComposerOnSent,
} from '../../compat/ui/composer.js';
